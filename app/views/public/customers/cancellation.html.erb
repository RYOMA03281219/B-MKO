<div class="container">
  <div class="row">
    <div class="col-md-7 mx-sm-auto shadow p-3 mb-5 bg-body rounded px-0 my-5 ">
      <h1 class="strong text-center text-title">※退会確認画面※</h1>

      <div class="mt-3 mb-3">
        <div class="text-center">
          退会すると、会員情報や</br>
          投稿した商品や購入履歴が削除されます。</br>
          退会する場合は、「退会」をクリックしてください。</br>
        </div>
      </div>

    <%= form_with model: @customer, url: customers_withdrawal_path, method: :patch do |f| %>
      <div class="form-group">
        <%= f.submit "退会実行", class: "btn btn-outline-danger btn-block", date:{confirm: "本当に退会されますか？"} %>
      </div>
        <%= link_to "戻る", customers_information_edit_path, class: "btn btn-outline-secondary btn-block" %>
    <% end %>
  </div>
</div>
